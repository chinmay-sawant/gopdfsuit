#set page(paper: "a4", margin: 1cm)
#set text(font: "Helvetica", size: 10pt)

// Load data from JSON
#let records = json("data.json")

= User Report

#table(
  columns: (0.5fr, 1.5fr, 2fr, 1.5fr, 3fr),
  table.header(
    [*ID*], [*Name*], [*Email*], [*Role*], [*Description*]
  ),
  ..records.map(r => (
    str(r.id),
    r.name,
    r.email,
    r.role,
    r.desc
  )).flatten()
)

#place(bottom + center, [Generated by Typst])
